spring:
  application:
    name: orderiq
  config:
    import: classpath:order-vocabulary.yaml
  sql:
    init:
      mode: always
  jackson:
    property-naming-strategy: SNAKE_CASE

orderiq:
  database:
    path: ${ORDERIQ_DB_PATH:./data/orders.db}
